import {View, Text} from "react-native"
import {SafeAreaView} from 'react-native-safe-area-context'

const Rides = () => {
    return (
        <SafeAreaView>
            <Text className="text-red-500">History</Text>
        </SafeAreaView>
    )
}

export default Rides;